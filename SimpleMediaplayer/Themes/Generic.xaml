<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleMediaplayer">

    <local:SystemInfo x:Key="Info"/>

    <Style TargetType="local:ControlBar" >
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:ControlBar">
                    <Grid x:Name="RootPanel" Background="Transparent" DataContext="{StaticResource Info}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="30"/>
                        </Grid.RowDefinitions>
                        <Grid x:Name="StateBar" Grid.Row="0" Background="{StaticResource DefaultControlBarBg}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="60"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="Filename" Foreground="{StaticResource DefaultControlBarText}" FontSize="{StaticResource DefaultControlBarTextSize}" Text="{TemplateBinding Filename}" 
                                       FontWeight="Light" Margin="10,0,0,0" Grid.Column="0" VerticalAlignment="Center"/>
                            <TextBlock x:Name="SysTime" Foreground="{StaticResource DefaultControlBarText}" FontSize="{StaticResource DefaultControlBarTextSize}" Text="{Binding HourMin}" 
                                       FontWeight="Light" Grid.Column="1" VerticalAlignment="Center"/>
                            <TextBlock x:Name="SysBattery" Foreground="{StaticResource DefaultControlBarText}" FontSize="{StaticResource DefaultControlBarTextSize}" Text="{Binding HourMin}" 
                                       FontWeight="Light" Grid.Column="2" VerticalAlignment="Center"/>
                        </Grid>
                        <RelativePanel x:Name="ControlBar" Grid.Row="1">
                            
                        </RelativePanel>
                        <Grid x:Name="SettingBar" Grid.Row="2" Background="{StaticResource DefaultControlBarBg}">
                            
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
